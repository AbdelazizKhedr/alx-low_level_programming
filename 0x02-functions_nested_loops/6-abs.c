#include "main.h"
/**
 * _abs -  computes the absolute value of an integer.
 * @n: the number to calculate absolute value for
 * Return: the absolute value for @n
 */
int _abs(int n)
{
	return (n >= 0 ? n : n * -1);
}
